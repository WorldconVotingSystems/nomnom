"""Waffle switch name constants for NomNom feature toggles.

These constants are convenience constants for the features we add
by default in an initial migration.
"""

SWITCH_ADVISORY_VOTES = "advisory_votes"
SWITCH_HUGO_PACKET = "hugo_packet"
