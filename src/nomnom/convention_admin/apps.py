from django.apps import AppConfig


class ConventionAdminConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "nomnom.convention_admin"
    verbose_name = "Convention Administration & Seeding"
